import Spinner from "../../../assets/img/Spinner.svg";
import React from "react";

let Preloader = (props)=>{
    return <div>
        <img src={Spinner} />
    </div>
}
export default Preloader